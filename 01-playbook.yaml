#playbook is list of plays, it should always starts with -
- name: ping the node
  hosts: mongodb
  #this is list of task/modules/collections
  tasks:
  - name: pinging the server
    # this map
    ansible.builtin.ping:
