#instaling multiple packages by using loops
- name: install packages
  hosts: mongodb
  become: yes
  tasks: 
  - name: install packages
    ansible.builtin.yum:
      name: "{{items}}"
      state: installed
    loop:
    - nginx
    - postmap
